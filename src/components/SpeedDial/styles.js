import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
    box:{
        position: 'fixed',
        top: 'auto',
        right: 100,
        bottom: 40,
        left: 'auto',
    },
    icon:{
        color: 'grey'
    },
}))